{% extends 'base.html.twig' %}

{% block title %}
	{{parent()}}
	- Catégories - Editer
{% endblock %}

{% block page_title %}
	<h1>Editer une catégorie
	</h1>
{% endblock %}

{% block body %}
	{{ include('category/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
	{{ include('category/_delete_form.html.twig') }}

	<a href="{{ path('category_show', {slug: category.slug}) }}" class="btn btn-secondary m-4" role="button">
		Retour
	</a>

{% endblock %}
