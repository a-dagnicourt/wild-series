<form method="post" action="{{ path('season_delete', {'programSlug': season.program.slug, seasonSlug: season.slug}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette saison ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ season.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
