{% form_theme form 'bootstrap_5_layout.html.twig' %}

{{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn btn-success my-4">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
