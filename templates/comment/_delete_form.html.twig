<form method="post" action="{{ path('actor_delete', {'id': actor.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet acteur ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actor.id) }}">
	<button class="btn btn-danger mb-5">Supprimer</button>
</form>
